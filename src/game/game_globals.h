#pragma once
#ifndef GAME_GLOBALS_H_
#define GAME_GLOBALS_H_

#define AUTON_DURATION 15000;
#define DRIVER_DURATION 105000;

enum class Flag {
  red = 1,
  blue = 2,
  none = -1,
};

enum class Alliance {
  red = 1,
  blue = 2,
  none = -1,
};

#endif